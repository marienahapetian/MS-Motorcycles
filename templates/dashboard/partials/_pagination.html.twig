<div class="mt-6 flex justify-center bg-indigo-200 py-3">
    <div class="flex gap-3 text-sm">
        {% for page in 1..totalPages %}
            <a href="#" class="px-3 py-1 {{ page == currentPage ? 'bg-white' : '' }}">
                {{ page }}
            </a>
        {% endfor %}
    </div>
</div>
