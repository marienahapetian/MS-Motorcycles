{{ form_label(form.categories, "Type de Produit", {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1' }}
) }}
{{ form_widget(form.categories, {
                    'attr': {
                        'class': 'w-full border bg-gray-100  px-3 py-2',
                        'data-action': 'change->product-features#changeCategory',
                        'data-product-id': product is defined ? product.id : null
                        }
                    }
                ) }}
<p class="text-red-500">
    {{ form_errors(form.categories) }}
</p>
